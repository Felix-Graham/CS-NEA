## Summary:
> Downloaded a maze image 
> Created actor classes for walls of different lengths
> Placed these along the displayed lines of the maze image
> Created an actor for the player with basic movement.

## Productive Content:
Scale images in Greenfoot with `getImage.scale(x, y)`. The example given used `getImage.getWidth()` for `x`, and the same but with `getHeight()` for `y`. 