## Summary
> Set random facing of direction each reload
> Created `public void changeImage()`
``` java 
public void changeImage(){
	num++
	setImage(num+".png");
}
```
> Actor moves around and changes image and direction when hits wall.
> Random speed as image changes